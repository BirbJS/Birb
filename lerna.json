{
  "packages": [ "packages/*" ],
  "version": "independent",
  "useWorkspaces": true,
  "command": {
    "publish": {
			"push": true,
			"registry": "https://registry.npmjs.org/"
		},
		"version": {
			"allowBranch": "main",
			"push": true,
      "createRelease": "github",
			"message": "chore(release): publish"
		}
  }
}
